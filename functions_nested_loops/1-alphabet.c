#include <stdio.h>

/**
 * main - entry point
 *
 * description - prints the alphabet
 *
 * Return: 0
 **/

void print_alphabet(void);

{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);

	putchar('\n');

return (0);
}
