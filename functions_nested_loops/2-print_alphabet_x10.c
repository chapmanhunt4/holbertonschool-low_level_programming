#include <stdio.h>

/**
 * print_alphabet - entry point
 *
 * description - prints the alphabet
 *
 * Return: void
 **/

void print_alphabet(void)
{
	char c;
	int n;

	for (c = 'a'; c <= 'z'; c++)

		while (n = 0; n < 10; n++)
			putchar(c);
	{
	putchar('\n');
	}
}
