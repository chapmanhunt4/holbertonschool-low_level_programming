#include "dog.h"
#include <stdio.h>

void print_dog(struct dog *d)
{
	char n = (nil)
	int i;

	if (d == NULL)
	{
		return
	}
	if (d)
	{
		for (i = 0; i < d; i++)
		{
			d[i] == NULL;
			printf("%c", n);
		}
	}
	if (name == NULL)
	{
		printf("Name: %c", n);
	}
}
